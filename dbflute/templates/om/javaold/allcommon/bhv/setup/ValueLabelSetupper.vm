${database.allClassCopyright}package ${glPackageBaseCommonBhvSetup};

import ${glPackageBaseCommon}.${glEntity};

/**
 * The interface of Value-Label Setupper.
 * ${database.filterGenericsParamOutput('<ENTITY>', 'The type of entity.')}
 * @author ${database.ClassAuthor}
 */
public interface ${glValueLabelSetupper}<ENTITY extends ${glEntity}> {

    /**
     * Set up value-label.
     * @param box Value-label box. (NotNull)
     * @param entity Entity. (NotNull)
     */
    public void setup(${glValueLabelBox} box, ENTITY entity);
}
