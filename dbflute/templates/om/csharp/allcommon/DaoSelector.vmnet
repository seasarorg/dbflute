
using System;
using System.Collections;

namespace ${glPackageBaseCommon} {

    public interface ${glDaoSelectorInterfaceName} {
        DAO_TYPE Select<DAO_TYPE>(Type daoType) where DAO_TYPE : ${glDaoReadableInterfaceName};
        ${glDaoReadableInterfaceName} ByName(String tableFlexibleName);
    }
}
