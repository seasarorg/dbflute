${database.allClassCopyright}package ${glPackageBaseCommonS2Dao};

/**
 * BeanMetaDataFactoryImpl for DBFlute.
 * 
 * @author ${database.ClassAuthor}
 */
public class ${glBeanMetaDataFactoryImpl} extends org.seasar.dao.impl.BeanMetaDataFactoryImpl {

    /**
     * Get the limit nest level of relation.
     * 
     * @return The limit nest level of relation.
     */
    protected int getLimitRelationNestLevel() {
#if ($database.isVersionAfter1047())
        return 3;// Extension for DBFlute
#else
	    return 2;// Extension for DBFlute
#end
    }
}