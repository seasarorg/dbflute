${database.allClassCopyright}package ${glPackageBaseCommonCBeanPagenavi};

#set ($myClassName = "${glPageNumberLinkSetupperName}")

/**
 * The setupper of page number link.
 * 
 * ${database.filterGenericsParamOutput('<LINK>', 'The type of link.')}
 * @author ${database.ClassAuthor}
 */
public interface ${myClassName}<LINK extends ${glPageNumberLinkName}> {

    /**
     * Set up.
     * 
     * @param pageNumberElement Page number element.
     * @param current Is current page?
     * @return Page number link. (NotNull)
     */
    public LINK setup(int pageNumberElement, boolean current);
}
