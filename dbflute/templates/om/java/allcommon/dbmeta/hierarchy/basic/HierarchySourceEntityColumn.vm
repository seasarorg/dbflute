${database.allClassCopyright}package ${glPackageBaseCommonDBMetaHierarchyBasic};

#set ($myClassName = "${glHierarchySourceEntityColumnName}")

import ${glPackageBaseCommonDBMetaHierarchy}.${glHierarchySourceColumnName};
import ${glPackageBaseCommonDBMetaInfo}.${glColumnInfoName};

/**
 * @author ${database.ClassAuthor}
 */
public class ${myClassName} implements ${glHierarchySourceColumnName} {

    protected ${glColumnInfoName} columnInfo;

    public ${myClassName}(${glColumnInfoName} columnInfo) {
        this.columnInfo = columnInfo;
    }

    public String getColumnName() {
        return columnInfo.getColumnDbName();
    }

    public java.lang.reflect.Method findGetter() {
        return columnInfo.findGetter();
    }
}