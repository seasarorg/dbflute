${database.allClassCopyright}package ${glPackageBaseCommonJdbc};

import java.util.List;

/**
 * The provider of latest SQL.
 * @author ${database.ClassAuthor}
 */
public interface ${glLatestSqlProviderName} {

    /**
     * Get display SQL.
     * @return Display SQL. (Nullable: If it was not found, returns null.)
     */
    public String getDisplaySql();

    /**
     * Extract the list of display SQL.
     * @return The list of display SQL. (NotNull: If it was not found, returns empty.)
     */
    public List<String> extractDisplaySqlList();

	/**
	 * Clear the cache of SQL.
	 */
	public void clearSqlCache();
}
